# Agent 折腾志 · 每日流程

Codex Scheduled Task 每天北京时间 06:00 照此执行。任务提示词只需要写：

> 读仓库根目录的 TINKER_DAILY.md，严格执行全部步骤。发布阶段显式使用 sites-building 和 sites-hosting，更新现有 Site，绝对不要创建新 Site。

**每一步失败都要在最终汇报里说明，不要静默跳过。**Claude Routine 与 Codex Scheduled Task
不能同时启用，否则会重复抓取并竞争写入 `seen.json`；启用本任务前先暂停旧 Routine。

这个项目要收的东西只有一种：**中文的、个人的、真的动过手的 Code / Work Agent 折腾记录**。
不收新闻、不收官方文档、不收论文、不收产品发布稿、不收「我做了个 X 求 Star」的纯自荐、
不收卖额度卖账号的软文。判断标准贯穿下面每一步，尤其是第 2 步。

---

## 0. 准备

    npm ci --omit=dev

**开工前先完整读 [`tinker/LESSONS.md`](tinker/LESSONS.md)**，再看 `tinker/REVIEW.md`
最近 3 条。前者是已经被数据验证过的长期结论（踩过的坑、走不通的死路、
以及「看起来合理但实测是错的」判断），后者是逐日流水。
不读 LESSONS 的直接后果是重走死路——里面明确列了机械去重、微信公众号、
知乎/CSDN/博客园搜索这些已经调查过不通的方向。

## 1. 抓取

    npm run tinker:fetch

产出 `tinker/data/_pending.json`，里面有：

- `shortlist` — 规则预筛入围的条目（默认上限 60，按源配额 8 席），带 `preScore`、`tools`、`preReasons`
- `rejected` — 被规则毙掉的，只留标题和判据
- `stats` / `perSource` / `errors` / `queries`

同时写 `tinker/data/_raw.json`（不进 git），是补全正文后、筛选前的完整快照。
以及 `tinker/data/query-yield.json`（**要提交**）——每个查询词跑过几轮、捞回多少条、
入围多少条的累计账。轮转池一周才转一圈，只有累计才能看出哪个词是真的长期零产出；
跑够 5 轮仍零入围的词会在抓取结束时打印出来，周更体检据此换词。

**如果大多数源返回同一种错误**（尤其全是 403），先 `curl -sI https://www.google.com` 判断是不是
环境网络策略问题。如果是环境问题：**不要提交 status.json 的失败计数，也不要写空的日期文件**，
直接在汇报里说明。「今天没有新内容」和「今天根本没能检查」是两回事。

## 2. 评审（这一步是这个项目的全部价值）

读 `tinker/data/_pending.json` 的 `shortlist`，逐条判断收不收。条目多时分批，每批 15-20 条。

### 收

- 作者**自己动手**做了什么，并写了过程、数字、结果。「我用 X 干了 Y，踩了 Z 个坑」
- 实测打脸型：某个说法/工具号称 A，实测是 B
- 工作流复盘：怎么把 agent 塞进真实开发流程，什么有效什么无效
- 深度拆解：读源码、扒架构、讲机制，不是转述文档
- 有意思的怪点子，哪怕规模很小

### 不收

- 新闻、发布公告、融资、榜单、协议更新播报
- **产品自荐帖**。这是论坛源的头号噪声，且和真折腾长得很像——两者都在讲「我做了个东西」。
  判据不是有没有做东西，而是**落点在哪**：落在「让你去用它」（功能列表、求 Star、
  欢迎试用、官网链接）就是自荐；落在「我为什么需要它、踩了什么坑」才是折腾。
  规则层已经扣了 6 分，但漏网的还很多，这一步要再挡一次
- 教程软文（判据：结尾在推某个中转站、镜像站、会员渠道）
- 求助帖、无内容的疑问帖、只有观点没有实践的讨论帖
- 论文解读、benchmark 报告
- 翻译或转载国外文章而无本人补充

### 评分与及格线

**只收 4 分及以上，3 分及以下一律不收。**

这条是 2026-08-01 创刊时定的，起因是第一版收了 29 篇，其中 13 篇是 3 分，
而那 13 篇几乎全是论坛的自荐帖和短讨论——整个列表因此显得很水。
宁可某天只有 3 篇，也不要用 3 分的东西凑数。

- **5 分**：读完能直接改变你明天怎么用 agent。有具体数字、具体坑、具体后果。每天最多 8 篇
- **4 分**：值得读完，有真实一手内容，但不是非读不可
- **3 分及以下**：不收。包括「有点意思但没什么信息量」「作者确实动手了但写得太薄」

**论坛帖（V2EX、linux.do、Discourse 等）额外一道关**：它得是一篇**写出来的东西**，
不是一条动态。三五行加个链接的，无论多贴题都不收。

**留意名单是不是被一个平台包了。** 供给现在几乎全靠搜索源，某一天入围名单
八成来自同一个平台是常态（实测有过 25/27 来自 SegmentFault）。
这本身不是错——那天可能只有它有货——但出刊时要有意识：
如果最后收的几篇全来自同一个平台、甚至同一个作者，
宁可少收一篇，也别让一期变成某个平台的专栏。

**同一选题只收最好的一篇。** 掘金上有内容农场，会把一个选题换五个标题反复发
（实测过一次：「Vibe Coding 胶水编程告别屎山」一天入围五条，占满了当天名单）。
它们文本各不相同，机械去重分不开（标题和正文的相似度都试过，
真重复的组内相似度反而低于真正不同的文章，见 REVIEW 2026-08-02），
所以**这一步只能靠你读**：名单里发现两条以上讲同一件事，留最好的那条，其余按重复毙掉。
判断依据是「读完第二篇还有没有新东西」，不是标题像不像。

**拿不准就不收。** 名单短一点没关系，混进去一篇软文会让整个列表贬值。

### 写什么

对每条**收**的条目，写：

- `titleZh` — 中文标题。原标题已经是中文就尽量沿用；太长或太含糊时重写，要具体
- `whyRead` — **一句话**说清「为什么值得点开」。这是全站最重要的一行字，
  用户靠它决定读不读。不要复述标题，要给出标题里没有的那个理由
- `summaryZh` — 3-6 句，**350-500 个字符**（`[...s].length`，数字、英文命令、标点都算）。
  说清这篇干了什么、结论是什么、有什么具体数字。不写「本文介绍了……」这种空话。
  **不用自己数**：`npm run build` 会把当天的条数、最短、中位、最长打出来，
  并逐条点名超区间的那几条。超了不报错——机制型选题（要装下多组数字或多段机制）
  确实会到 550 上下，但它该是一天里的一两条，不是默认值。
  （这个区间改过三次，前三次都是拿实测分布对齐实践却始终对不上，REVIEW 为此连记了五天。
  2026-08-30 复量才发现问题不在数字在**单位**：手册只写「250-450 字」，没写一个「字」是什么，
  而同一批 08-29 的条目按全部字符中位数是 503、按汉字数是 239——
  一种读法说几乎每条都超上限，另一种说几乎每条都不到下限，同一份语料两个相反结论。
  所以这次先把单位钉死成全部字符，再按同一把尺子重取区间
  ——近 14 天 96 条 P10 369、中位 436、P75 486——并把量这件事交给构建期，
  免得下一次复盘又从记忆里重新量一遍。下限从来没构成过约束，被撞的一直是上限。）
- `rating` — 1-5 整数。5 分是「今日精选」，页面会打标，**每天最多给 8 篇 5 分**，
  给多了这个标就没有意义了
- `tools` — **工具（产品）** id 数组：Claude Code、Codex CLI、Cursor、OpenClaw、Manus 这类
- `topics` — **话题（概念/实践）** id 数组：MCP、CLAUDE.md、Agent Skills、Vibe Coding、
  规格驱动开发、Loop Engineering 这类

  两者是页面上两组独立的筛选器，**不要混**。判据很简单：能不能安装、能不能打开？
  能就是工具，不能就是话题。抓取时会给出候选，评审时按这条判据核一遍再增删——
  规则匹配会误判（词表里的中文别名曾把「核**心流**程」判成 iFlow）。
  语料里出现了词表没有的产品或概念，加进 `lib/tinker/vocab.mjs` 的 `TOOLS` / `TOPICS`

**`thin: true` 的条目抓不到正文，只能依据标题判断，绝对不许编造内容、数字或结论。**
这类条目页面会显示「⚠ 仅标题」。拿不准就直接不收——折腾志不像信源罗盘要求覆盖度，
少一条没有任何损失。

### 写进哪

写 `tinker/data/<date>.json`。

**如果这个文件已经存在，读出来往 `items` 里追加，不要整个覆盖。**
同一天可能被跑不止一次（手动补跑、上一轮漏评的候选补评），
直接覆盖会把先前收录的条目静默抹掉。追加时按 `url` 去重，
`reviewed` / `dropped` 累加，`dailyNote` 重写成能涵盖两批的说法。

格式：

```json
{
  "date": "YYYY-MM-DD",
  "generatedAt": "<ISO 时间>",
  "dailyNote": "<编者按，2-4 句>",
  "items": [ { "id": "", "source": "", "kind": "", "url": "", "titleOriginal": "",
               "titleZh": "", "summaryZh": "", "whyRead": "", "rating": 1,
               "tools": [], "topics": [], "publishedAt": "", "author": "", "preScore": 0 } ],
  "reviewed": 0, "dropped": 0, "stats": {}, "errors": []
}
```

`id`/`url`/`publishedAt`/`source`/`kind`/`author`/`titleOriginal` 从 shortlist 原样抄，不要改。
`items` 按 `rating` 降序、同分按发布时间降序。

`dailyNote`（编者按）不是摘要汇总，是**今天这批的编辑视角**：今天什么话题扎堆、
哪篇最值得先读、毙掉了什么以及为什么。写得像个人说话，别写成播报。

当天一条都没收也要写出 `items: []` 的日文件，让日历上出现这一天，
并在 `dailyNote` 里说明是「今天确实没有值得收的」还是「抓取出了问题」。

## 3. 构建

    npm test
    npm run build

必须同时产出 `dist/index.html`、`dist/tinker.html` 和 `dist/server/index.js`。完整构建会把
折腾志原页面原样挂到 Site 的 `/tinker` 路径；构建期会校验字段，缺字段或 rating 不是 1-5
会直接报错并指出是哪条。构建失败时不得提交或发布。

## 4. 提交 GitHub

只提交本次每日运行产生的预期变更。提交信息：

    折腾志 <date>：收录 <K> 篇

执行 `git push origin main`。若推送失败，停止发布并汇报；否则下一天可能重复收录。

## 5. 发布现有 Codex Site

显式使用 `sites-building` 与 `sites-hosting`：

1. 读取 `.openai/hosting.json` 并复用其中的 `project_id`；绝对不要创建新 Site。
2. 保留现有 D1 绑定、AI 信源罗盘首页和当前访问范围。
3. 用已经测试并提交的同一份源码保存一个新 Sites 版本并部署。
4. 轮询到部署成功或失败；失败时说明错误，不要声称已发布。
5. 成功后确认生产地址的 `/tinker` 路径可访问。

## 6. 挖新信源（不能跳过）

这个系统的天花板由信源决定，而中文长尾博客是挖不完的。**每次运行都要做这一步。**

1. 用 WebSearch 跑 3-5 个查询，每次换不同角度，不要重复用同一批词。参考方向：
   具体工具名 + 中文实践词（`Claude Code 踩坑 博客`、`Codex CLI 心得 记录`）、
   还没进词表的新工具、中文技术周刊、某个已收录作者的友链页、
   「XX 折腾」「XX 使用一个月」这类句式
2. 从结果里挑出**个人博客域名**（不是掘金/知乎这类平台的文章页，那些已经被搜索源覆盖）
3. 把候选站点首页写进 `tinker/candidates.txt`，跑
   `node scripts/tinker-probe.mjs --file tinker/candidates.txt`
4. 探测到 feed、且最新文章在 365 天内的，加进 `tinker/sources.json`
   （`kind: "blog"`，写一句具体的 `desc`，别写「中文技术博客」这种废话）
5. 顺便看一眼 `lib/tinker/vocab.mjs`：今天的条目里有没有出现词表里没有的产品或概念？
   产品加进 `TOOLS`，概念加进 `TOPICS`，并考虑给 `SEARCH_QUERIES` 补一条对应查询。
   **加中文别名前先自问它会不会是某个常用词的一段**——中文没有词边界，
   两个字的别名基本都会误伤（「心流」命中「核心流程」，「扣子」命中「纽扣子」）

另有一条**批量扩源**的路子，效果比搜索引擎好得多（搜索引擎搜个人博客基本无效，
出来的全是 CSDN / 知乎这类 SEO 平台页）：

    node scripts/tinker-harvest.mjs               # 只看结果
    node scripts/tinker-harvest.mjs --merge       # 确认过命中列表之后再并入
    node scripts/tinker-harvest.mjs --min-hits 2 --days 180   # 放宽门槛

它同时吃四个社区维护的索引（CSV / OPML / Markdown 格式各异），筛出
「近期活跃 + 不是媒体节奏 + 近 20 篇里至少 2 篇能过收录线」的 feed。
判据直接调用系统自己的 `scoreItem()`，和日常筛选完全一致。

**`--merge` 之前必须先看一遍命中列表，不要图快直接合。**
有一类源机器判不出来：大厂官方技术号（腾讯技术工程、小米技术这种，
通过 wechat2rss 的 feed 进来）写的文章确实像实践文，`scoreItem()` 拦不住，
但它们是「官方和正式的东西」，正是这个项目要排除的。只能靠人扫一眼标题。

**准入标准严，留存标准松，两者不要混用。**
2026-08-02 拿新标准回审上一轮自动加入的 84 个源，只有 39 个还够格；
但那 45 个「不够格」的源里，有 3 个当天各贡献了一篇已发布文章，
其中 sjdhome blog 那篇还是 5 分精选。一个月更博主一年出一篇好文，
留着它的成本只是每天一次 HTTP 请求，砍掉的代价却是那篇好文再也进不来。
所以：**新源按严标准准入，已有源只按「连续 30 天零入选且 feed 中期文章完全不沾 agent」清理**。

`--min-span` 是区分个人博客与媒体的闸：媒体日更十几条，近 20 篇可能只跨两三天。
这一条是踩出来的——从「订阅数最多」的榜单里捞源，出来的全是 36氪、钛媒体、
爱范儿、极客公园，它们每天报道 AI，旧判据「提没提 agent」轻松达标。
**手工添加的平台型源（掘金、博客园、iT 邦幫忙）不受这条约束**，它们本来就是聚合站，
标 `kind: "aggregator"` 即可。

**每次至少要新增 1 个源或 1 个词条。** 一个都没加，说明这一步没有认真做。
连续找不到新东西时，换搜索语言（试试用工具的英文名搜中文页面）、换平台
（Discourse 论坛、中文 newsletter 平台、GitHub 中文 awesome 仓库的友链）。

## 7. 自我复盘（不能跳过）

往 `tinker/REVIEW.md` 追加今天这条（流水账）：

```
## YYYY-MM-DD
抓取 N 条 → 规则入围 M 条 → 收录 K 条（收录率 K/M）
来源分布：...
毙掉的主要类型：...
本次改动：...
```

然后**至少提出并实施一项具体改进**。可以质疑之前任何一次的决定，包括我写下的规则。
值得看的信号：

- **收录率过低（<30%）** → 规则太松，噪声进太多。看 `rejected` 里被毙的和
  `shortlist` 里被你毙的分别长什么样，收紧 `lib/tinker/relevance.mjs` 的权重或加反向词
- **收录率过高（>80%）** → 规则太紧，可能在漏东西。跑
  `node scripts/tinker-retriage.mjs --rejected` 看被规则毙掉的高分条目里有没有误杀
- **某个源连续 21 天零收录** → 在汇报里点名，建议停用（改 `enabled: false`），
  但别自己直接停博客源——个人博客本来就月更，零产出很正常，
  只有明显跑偏主题的源才值得停
- **某个源连续失败 ≥7 天** → 点名，让用户决定修 feed 还是停用
- **入围名单被某一个源占了一半以上** → 调 `PER_SOURCE_QUOTA`
- **搜索查询命中率** → `perSource` 里搜索源产出为 0，说明当天那批查询词没用，
  考虑替换 `SEARCH_QUERIES` 里的低效条目

改完跑 `npm test` 确认没弄坏东西。规则改动可以用
`node scripts/tinker-retriage.mjs` 离线对比效果，不用重新抓取。

**如果今天的结论是长期有效的，把它提炼进 `tinker/LESSONS.md`**——
判据是「半年后还用得上吗」。逐日数字留在 REVIEW，
「某条路走不通」「某个判据是错的」这类要进 LESSONS，否则会被流水淹没。
注意 `lib/html-text.mjs`、`lib/feed-parse.mjs`、`lib/store.mjs` 是和信源罗盘
**共用**的，动它们要跑全量测试。

## 8. 汇报

一段话说明：

- 抓了多少、入围多少、收录多少，收录率
- 今天最值得读的一两篇是什么，为什么
- 毙掉的主要是哪类
- 新加了什么信源 / 词条（第 6 步）
- 做了什么改进（第 7 步）
- 抓取失败的源，有没有连续失败 ≥7 天的（点名）
- 测试、构建与 GitHub 推送是否成功
- Codex Site 是否发布成功及生产链接
