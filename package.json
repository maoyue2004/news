{
  "name": "ai-news-reader",
  "version": "1.0.0",
  "type": "module",
  "private": true,
  "engines": { "node": ">=22" },
  "scripts": {
    "test": "node --test test/",
    "fetch": "node scripts/fetch.mjs",
    "build": "node scripts/build.mjs"
  },
  "dependencies": {
    "fast-xml-parser": "^4.5.0"
  }
}
